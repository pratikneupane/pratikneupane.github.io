export type HeroProps = {
  isDarkMode: boolean;
};

export type HeaderProps = {
  isDarkMode: boolean;
  handleDarkMode: () => void;
};

export type IconContainerProps = {
  isDarkMode: boolean;
  icon: any;
};